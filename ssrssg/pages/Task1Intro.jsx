export default function Task1Intro() {
  return (
    <section>
      <h2>1. Introduction to SSR & SSG</h2>
      
      <p>
        <strong>SSR (Server-Side Rendering):</strong> Server-Side Rendering
        (SSR) is a technique where a web page is generated on the server at
        request time and sent to the client with fully populated HTML content.
        This improves SEO and initial page load performance.
        <br />
        <strong>SSG (Static Site Generation):</strong> Static Site Generation
        (SSG) pre-renders pages at build time and serves them as static HTML
        files.
      </p>
    </section>
  );
}
